{
  "name": "browser-mcp",
  "version": "0.1.0",
  "description": "MCP server giving agents a real browser — navigate, interact, capture, and inspect via Playwright. Extensible with domain plugins.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "browser-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "keywords": [
    "mcp",
    "browser",
    "playwright",
    "agent",
    "automation",
    "screenshot"
  ],
  "license": "PolyForm-Strict-1.0.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "pixelmatch": "^6.0.0",
    "playwright": "^1.52.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pngjs": "^6.0.5",
    "typescript": "^5.7.0"
  }
}
